<?php
    /**
     * These are on / off switches for extended functionality such as custom post types, taxonomies etc.
     * If you disable some function, its core file (found at /<theme folder>/functions/<function>.php) won't be included.
     *
     * All options are turned on by default
     */

    /**
     * Use sidebars
     */
    $use_sidebars = true;

    /**
     * Custom post types
     */
    $use_custom_post_types = true;

    /**
     * Taxonomies
     */
    $use_custom_taxonomies = true;

    /**
     * Image Sizes
     */
    $use_custom_image_sizes = true;

    /**
     * Theme Options
     */
    $use_theme_options = true;
?>